apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: download-pvc
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 300Gi
  storageClassName: nfs-client
