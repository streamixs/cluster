apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: twingate

generators:
  - generators/secret-generator.yaml

resources:
  - deployment.yaml
  - service.yaml
  - config/config.yaml
  - secret/secret.yaml