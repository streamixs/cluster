apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: streamixs-com-wildcard
  namespace: traefik
spec:
  secretName: streamixs-com-staging-tls
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  commonName: "*.streamixs.com"
  dnsNames:
    - "streamixs.com"
    - "*.streamixs.com"
