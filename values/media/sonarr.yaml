image:
  repository: lscr.io/linuxserver/sonarr
  tag: 3.0.10

service:
  main:
    ports:
      http:
        port: 8989

ingress:
  main:
    hosts:
      - host: sonarr.streamixs.com
        paths:
          - path: /
            pathType: Prefix
            service:
              name: main
              port: http
    tls:
      - secretName: sonarr-tls
        hosts: [sonarr.streamixs.com]

persistence:
  config:
    enabled: true
    storageClass: <STORAGE_CLASS>
    accessMode: ReadWriteOnce
    size: 5Gi
    mountPath: /config
  downloads:
    enabled: true
    type: pvc
    existingClaim: <OPTIONAL_EXISTING_DOWNLOADS_PVC>
    storageClass: <STORAGE_CLASS>
    size: 200Gi
    accessMode: ReadWriteOnce
    mountPath: /downloads
  media:
    enabled: true
    type: pvc
    existingClaim: <OPTIONAL_EXISTING_MEDIA_PVC>
    storageClass: <STORAGE_CLASS>
    size: 1Ti
    accessMode: ReadWriteMany
    mountPath: /media

controllers:
  main:
    containers:
      main:
        env:
          PUID: <PUID>
          PGID: <PGID>
          TZ: Europe/Paris
        probes:
          liveness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /
                port: 8989
              initialDelaySeconds: 30
          readiness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /
                port: 8989
              initialDelaySeconds: 15

serviceMonitor:
  enabled: true
  endpoints:
    - port: http
      interval: 30s
